#include "../header/StringProblems.h"

void MaxRepetatingCharacter(std::string inp_string)
{
	/*int count[26] = {0};

	for (int i = 0; i < inp_string.length(); i++)
		count[inp_string.at(i)]++;
	*/

}